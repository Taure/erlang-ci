name: Self Release

on:
  workflow_run:
    workflows: [Test]
    types: [completed]
    branches: [main]

jobs:
  release:
    if: github.event.workflow_run.conclusion == 'success'
    uses: ./.github/workflows/release.yml
    permissions:
      contents: write
    with:
      floating-major-tag: true
